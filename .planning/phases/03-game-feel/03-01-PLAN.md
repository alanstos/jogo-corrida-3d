---
phase: 03-game-feel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - game/Game.js
  - game/Car.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Production build has no periodic console.log spam during gameplay"
    - "Game.js no longer counts frames just to emit a diagnostic log"
    - "Car.js no longer logs construction diagnostics to console"
  artifacts:
    - path: "game/Game.js"
      provides: "Game loop without _frameCount diagnostic logging"
      contains_not: "console.log"
    - path: "game/Car.js"
      provides: "Car constructor without init diagnostic log"
      contains_not: "console.log('Car init'"
  key_links:
    - from: "game/Game.js _tick()"
      to: "console (browser)"
      via: "REMOVED — no log call inside _tick"
      pattern: "no console.log inside _tick"
---

<objective>
Remove the console.log technical debt accumulated during v1.0 development. Two diagnostic logs persist in production: a periodic body.position log fired once per ~60 frames inside Game._tick (Game.js L273-277) and a Car-init log at Car.js L29-34. Both were left in after the bug investigation that produced v1.0; they now constitute console noise that obscures real errors and adds CPU overhead on every 60th frame.

Purpose: Wave-1 cleanup that unblocks Wave-2 plans. Removing this noise now means subsequent plans (turbo, particles, shake, haptic) start from a clean console — any new log that appears during their development is automatically a regression signal.

Output: Game.js and Car.js with all diagnostic console.log calls removed; production build emits zero periodic logs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-game-feel/03-RESEARCH.md
@.planning/phases/03-game-feel/03-PATTERNS.md
@game/Game.js
@game/Car.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Game.js periodic frame-count log</name>
  <files>game/Game.js</files>
  <read_first>
    - game/Game.js (full file — see current constructor at L11-16 and _tick at L228-278)
    - .planning/phases/03-game-feel/03-PATTERNS.md (section "game/Game.js — MODIFY", subsection "Console.log debt to remove" — quotes the exact 4 lines to delete and confirms the L16 constructor field to remove)
    - .planning/phases/03-game-feel/03-RESEARCH.md (section "Code Examples — Console.log Removal (Plan 03-01)" — confirms removal scope)
  </read_first>
  <action>
    Delete the periodic diagnostic log block from Game._tick. The block currently spans Game.js L273-277:
    - L273: `this._frameCount++;`
    - L274: `if (this._frameCount % 60 === 0) {`
    - L275: `  const p = this.car.body.position;`
    - L276: `  console.log(\`[frame ${'$'}{this._frameCount}] body.position:\`, { x: p.x.toFixed(3), y: p.y.toFixed(3), z: p.z.toFixed(3) });`
    - L277: `}`

    Delete all 5 lines. They must NOT be replaced by any other log, counter, or guard — the entire block is gone.

    Then remove the field initializer `this._frameCount = 0;` from the Game constructor at L16. Leave surrounding constructor fields (`this._rafHandle`, `this._lastTime`, `this._paused`, `this.state = 'MENU'`) untouched.

    After these deletions, search Game.js once more for `console.log` and `_frameCount`. The file MUST contain zero occurrences of either identifier. If a different `console.log` exists elsewhere in Game.js (e.g. inside the webglcontextrestored catch block at L110 which uses `console.error`), leave `console.error` alone — only `console.log` is being removed.

    Do not modify Game._tick step ordering, do not modify any state-machine logic, do not modify the score calculation. The render call (`this.renderer.render(this.scene, this.camera.instance)` at L271) remains the last statement of the function.
  </action>
  <verify>
    <automated>grep -n "console.log" game/Game.js | grep -v "^\s*//" ; test $(grep -c "console\.log" game/Game.js) -eq 0 ; grep -c "_frameCount" game/Game.js | awk '$1!=0{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "console.log" game/Game.js` outputs `0`
    - `grep -c "_frameCount" game/Game.js` outputs `0`
    - Game.js still contains `this.renderer.render(this.scene, this.camera.instance);` as the last statement of `_tick`
    - Game.js _tick still contains the existing step order markers: `this.controls.getIntent()`, `this.car.applyInput(intent)`, `this.track.update`, `this.physicsWorld.step(safeDt)`, `this.car.syncMesh()`, `this.camera.follow`, `this.hud.update`, `this.renderer.render` — in that exact order
    - `console.error` calls inside Game.js (specifically the webglcontextrestored catch handler) remain unchanged
  </acceptance_criteria>
  <done>
    Game.js contains no `console.log` calls and no `_frameCount` field; _tick body is purely the game-loop steps and the render call.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Car.js init diagnostic log</name>
  <files>game/Car.js</files>
  <read_first>
    - game/Car.js (full file — see constructor at L9-35 and the diagnostic log at L29-34)
    - .planning/phases/03-game-feel/03-PATTERNS.md (section "game/Car.js — MODIFY", subsection "Console.log debt to remove" — quotes the exact 5 lines to delete)
    - .planning/phases/03-game-feel/03-RESEARCH.md (section "Open Questions" #3 + section "Assumptions Log" A4 — both confirm Car init log should be removed as part of Plan 03-01 clean sweep)
  </read_first>
  <action>
    Delete the Car constructor init diagnostic. Current location Car.js L28-34, including the preceding two-line comment:
    - L28: `// Diagnostic — verify mass, type, allowSleep before any input is wired`
    - L29: `// PITFALLS #4: mass=0 -> static, type=2. Expected: mass=150, type=1 (DYNAMIC), allowSleep=false`
    - L30: `console.log('Car init:', {`
    - L31: `  mass: this.body.mass,`
    - L32: `  type: this.body.type,`
    - L33: `  allowSleep: this.body.allowSleep,`
    - L34: `});`

    Delete all 7 lines (both the explanatory comments and the log statement). The constructor's preceding block — physicsWorld.addBody(this.body), scene.add(this.mesh), the collide event listener at L24-26 — remains untouched.

    Do not modify the body construction (`this.body = this._buildBody()` at L14), do not modify the mesh construction, do not modify the collide event wiring. The constructor concludes after the collide event listener with no further statements.

    After deletion, the file MUST contain zero `console.log` calls. (Car.js has no `console.error` calls.)
  </action>
  <verify>
    <automated>test $(grep -c "console\.log" game/Car.js) -eq 0 ; test $(grep -c "Car init:" game/Car.js) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "console.log" game/Car.js` outputs `0`
    - `grep -c "Car init:" game/Car.js` outputs `0`
    - Car constructor still calls `physicsWorld.addBody(this.body)` and `scene.add(this.mesh)`
    - Car constructor still wires `this.body.addEventListener('collide', ...)` with the existing delegation pattern (delegates to `this._onCollide`)
    - `this.body = this._buildBody()` and `this.mesh = this._buildMesh()` remain at L14-15
  </acceptance_criteria>
  <done>
    Car.js constructor ends after the collide event listener with no diagnostic log; the file contains zero `console.log` calls.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify zero periodic logs across game/ directory</name>
  <files>game/Game.js, game/Car.js, game/Camera.js, game/Controls.js, game/HUD.js, game/Track.js, game/PhysicsWorld.js, game/Storage.js</files>
  <read_first>
    - game/Game.js (post-Task-1 state — confirm console.log absent)
    - game/Car.js (post-Task-2 state — confirm console.log absent)
    - game/Storage.js (confirm whether its existing logging is `console.warn`/`console.error` only, not `console.log`)
  </read_first>
  <action>
    Run a sweep across all 8 files in game/. The objective is to confirm no `console.log` survives outside of intentional error/warn paths.

    Procedure:
    1. Run `grep -rn "console\.log" game/`. Expected output: zero matches.
    2. If any `console.log` remains in any file other than Game.js and Car.js (the only two files Tasks 1-2 modified), STOP and report — this is unexpected new debt that was not part of the cleanup scope. The plan's verification target is "zero console.log in game/", but only Game.js and Car.js were modified by Tasks 1-2; finding extra logs means the brief understated the debt and needs follow-up.
    3. Run `grep -rn "console\.error\|console\.warn" game/` and confirm any matches are inside legitimate error-handling paths (e.g. the webglcontextrestored catch in Game.js, or Storage.js try/catch fallbacks). Do NOT remove `console.error` or `console.warn` calls — those are intentional.
    4. Confirm `grep -c "_frameCount" game/Game.js` is `0` (regression check on Task 1).

    Do not modify any files in this task. This is verification-only — Tasks 1 and 2 do all the editing.
  </action>
  <verify>
    <automated>test $(grep -rn "console\.log" game/ | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "console.log" game/` returns no matches (exit code 1, zero lines)
    - `grep -c "_frameCount" game/Game.js` returns `0`
    - `grep -rn "console.error\|console.warn" game/` may return matches, but each match must be inside a try/catch or visible error-handling block (subjective check is allowed here — the test is "no surprise console.log", not "no console at all")
  </acceptance_criteria>
  <done>
    Zero `console.log` calls remain anywhere under game/; production gameplay emits no periodic console output; `_frameCount` field is fully removed from Game.js.
  </done>
</task>

</tasks>

<verification>
After all three tasks, start the dev server (`npm run dev`), open the game in a browser, click INICIAR, play for ~10 seconds, and watch the DevTools Console. There must be:
- Zero `[frame N] body.position` messages
- Zero `Car init:` messages
- No new errors or warnings introduced by the deletions

Run `npm run build` to confirm the production bundle still builds cleanly with no syntax errors caused by the deletions.
</verification>

<success_criteria>
1. `grep -rn "console.log" game/` returns zero matches.
2. `grep -c "_frameCount" game/Game.js` returns `0`.
3. `npm run build` succeeds without errors.
4. Game still starts and plays end-to-end (sanity check — no constructor field referenced elsewhere was accidentally removed).
</success_criteria>

<output>
Create `.planning/phases/03-game-feel/03-01-SUMMARY.md` when done.
</output>

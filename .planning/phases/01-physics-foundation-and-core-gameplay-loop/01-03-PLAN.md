---
phase: 01-physics-foundation-and-core-gameplay-loop
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - game/HUD.js
  - game/Controls.js
  - game/Game.js
  - index.html
  - style.css
autonomous: false
requirements: [CTRL-01, CTRL-03, HUD-01, HUD-02]

must_haves:
  truths:
    - "While playing, a HUD overlay shows SCORE and SPEED, both updating every frame in a pixel-style font"
    - "On a touch device, two fixed bottom-corner buttons (LEFT and RIGHT) drive the car when held; pressing both at once works (multi-touch)"
    - "Touch buttons show a visible press-state (highlight/scale) when active"
    - "Keyboard fallback (ArrowUp/Left/Right, W/A/D) continues to work for desktop testing"
    - "On a real Android device opened via the dev URL, touching the left/right buttons makes the car visibly steer — the car never stays still on touch input"
  artifacts:
    - path: "game/HUD.js"
      provides: "DOM overlay reader that updates SCORE and SPEED textContent each frame"
      contains: "textContent"
    - path: "index.html"
      provides: "HUD overlay DOM (#hud) + touch button DOM (#btnLeft, #btnRight)"
      contains: "btnLeft"
    - path: "style.css"
      provides: "Pixel font (Press Start 2P), neon HUD styling, large fixed touch buttons with active state"
      contains: "Press Start 2P"
    - path: "game/Controls.js"
      provides: "Per-pointerId multi-touch state + keyboard fallback + button press feedback hooks"
      contains: "pointerdown"
    - path: "game/Game.js"
      provides: "HUD instance wired to loop; hud.update(state) after camera, before render"
      contains: "hud.update"
  key_links:
    - from: "game/Controls.js"
      to: "DOM #btnLeft / #btnRight"
      via: "pointerdown / pointerup / pointercancel listeners tracking per-pointerId state"
      pattern: "pointerdown.*pointerId"
    - from: "game/Game.js"
      to: "game/HUD.js"
      via: "hud.update({ score, speed }) called each frame in PLAYING state"
      pattern: "hud\\.update"
    - from: "DOM buttons"
      to: "visual press state"
      via: "CSS :active or JS .pressed class toggled in Controls"
      pattern: "pressed|:active"
---

<objective>
Vertical slice: turn the game into something a mobile player can actually play. This plan adds the DOM HUD overlay (SCORE + SPEED in pixel font), the fixed-bottom touch button controls with multi-touch support and visible press feedback, the pixel-font typography, and proves end-to-end that a real Android device opened on the LAN dev URL renders the game and responds to touch.

Purpose: Without this plan, the game is desktop-only and has no in-game feedback. The Phase 1 success criterion #4 ("Score and speed are visible in the HUD throughout the run with a pixel font") and #1 ("On a real Android device, touching the left or right button causes the car mesh to visibly move and steer") are both delivered here.

Output: A mobile-ready playable build with full HUD, touch controls, keyboard fallback, and on-device verification.
</objective>

## Phase Goal

**As a** real player holding an Android phone, **I want to** open the LAN dev URL, tap the left/right buttons at the bottom of the screen, see my score and speed update live in a pixel font HUD, and play a full crash-retry loop using only touch, **so that** RetroRacer is genuinely a mobile game and not a desktop demo.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-physics-foundation-and-core-gameplay-loop/01-01-SUMMARY.md
@.planning/phases/01-physics-foundation-and-core-gameplay-loop/01-02-SUMMARY.md
@game/Game.js
@game/Controls.js
@game/Car.js
@game/Track.js
@index.html
@style.css
</context>

<tasks>

<task id="01-03-T1" type="execute" tdd="false">
  <name>Task 1: HUD DOM overlay (SCORE + SPEED, pixel font)</name>
  <files>index.html, style.css, game/HUD.js, game/Game.js</files>
  <read_first>
    - .planning/research/FEATURES.md "HUD Elements" table — Phase 1 needs SCORE (top center) and SPEED (top or bottom). Defer turbo bar, star progress, phase indicator to Phase 2.
    - .planning/research/ARCHITECTURE.md "HUD" row in Component Boundaries — DOM writes only, reads game state scalars, no Three.js involvement. "Physics → HUD" data flow.
    - inicio.md "Tema visual" — Press Start 2P font from Google Fonts. Neon palette: roxo #1a0033, ciano #00ffff, amarelo #ffee00, vermelho #ff2255.
    - game/Game.js — `this.score` already updated by 01-02 T3. Speed source: `this.track._scrollSpeed` (but that's private — add a public `getSpeed()` on Track that returns Math.floor(this._scrollSpeed) for clean access).
    - game/Track.js — add the `getSpeed()` accessor here in T1.
  </read_first>
  <action>
    **Modify `index.html`:**
    - In `<head>`, add `<link rel="preconnect" href="https://fonts.googleapis.com">` and `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap">`. This load is one-time and small.
    - In `<body>` BEFORE the existing `#gameOver` overlay, add the HUD overlay:
    ```html
    <div id="hud" class="hud" aria-hidden="true">
      <div class="hud-cell hud-score">SCORE <span id="hudScore">0</span></div>
      <div class="hud-cell hud-speed">SPEED <span id="hudSpeed">0</span></div>
    </div>
    ```
    `aria-hidden=true` because HUD content is purely visual; screen readers ignore it.

    **Modify `style.css`:**
    - Add a base rule: `body, button, .hud, .overlay { font-family: 'Press Start 2P', monospace; }` — falling back to monospace if the Google Font fails (mobile may be offline).
    - Add HUD styles:
    ```
    .hud {
      position: fixed;
      top: 0; left: 0; right: 0;
      display: flex;
      justify-content: space-between;
      padding: 12px 16px;
      pointer-events: none;
      z-index: 50;
      color: #00ffff;
      font-size: 14px;
      text-shadow: 0 0 8px #00ffff;
    }
    .hud-cell { padding: 4px 8px; }
    .hud-score { color: #ffee00; text-shadow: 0 0 8px #ffee00; }
    .hud-speed { color: #00ffff; text-shadow: 0 0 8px #00ffff; }
    @media (max-width: 480px) {
      .hud { font-size: 11px; padding: 8px 10px; }
    }
    ```
    `pointer-events: none` is critical — the HUD must never block touch events from reaching the canvas or the touch buttons below.

    **Modify `game/Track.js`:** Add `getSpeed() { return Math.floor(this._scrollSpeed); }` (avoids exposing the private field; returns an integer for clean HUD display).

    **Create `game/HUD.js`:**
    ```
    export default class HUD {
      constructor() {
        this._scoreEl = document.getElementById('hudScore');
        this._speedEl = document.getElementById('hudSpeed');
        this._lastScore = -1;
        this._lastSpeed = -1;
      }
      update(state) {
        // state = { score, speed }
        const s = Math.floor(state.score);
        if (s !== this._lastScore) {
          this._scoreEl.textContent = String(s);
          this._lastScore = s;
        }
        if (state.speed !== this._lastSpeed) {
          this._speedEl.textContent = String(state.speed);
          this._lastSpeed = state.speed;
        }
      }
    }
    ```
    The `_lastScore`/`_lastSpeed` diff guard avoids redundant DOM writes (which trigger style recalculation 60x/sec otherwise) — meaningful on mobile.

    **Modify `game/Game.js`:**
    - Import and instantiate: `this.hud = new HUD()` in constructor (after DOM is ready, which is already ensured per 01-02).
    - In `_tick` PLAYING branch (NOT the GAME_OVER early-return), AFTER `this.camera.follow(...)` and BEFORE `this.renderer.render(...)`, call:
      ```
      this.hud.update({ score: this.score, speed: this.track.getSpeed() });
      ```
    - HUD is also visible during GAME_OVER (the overlay sits on top of it). No special handling needed.

    Loop step ordering after this task:
    1. RAF → 2. delta cap → 3. pause guard → 4. state guard (GAME_OVER short-circuit) → 5. read intent → 6. car.applyInput → 7. track.update → 8. world.step → 9. car.syncMesh → 10. camera.follow → 11. hud.update → 12. renderer.render
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('fs');const c=[['index.html includes hudScore',fs.readFileSync('index.html','utf8').includes('id=\"hudScore\"')],['style.css includes Press Start 2P',fs.readFileSync('style.css','utf8').includes('Press Start 2P')],['style.css HUD pointer-events none',fs.readFileSync('style.css','utf8').includes('pointer-events: none')],['HUD.js exists',fs.existsSync('game/HUD.js')],['Game.js calls hud.update',fs.readFileSync('game/Game.js','utf8').includes('hud.update')],['Track.js exposes getSpeed',fs.readFileSync('game/Track.js','utf8').includes('getSpeed()')]];let bad=0;c.forEach(([n,ok])=>{console.log(ok?'OK':'MISS',n);if(!ok)bad++});process.exit(bad?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` contains `id="hudScore"` and `id="hudSpeed"` inside a `#hud` div
    - `index.html` loads Press Start 2P from Google Fonts
    - `style.css` includes `font-family: 'Press Start 2P'` and `pointer-events: none` on `.hud`
    - `game/HUD.js` exists with `update(state)` method that uses textContent diff guards
    - `game/Game.js` calls `this.hud.update({ score, speed })` once per PLAYING tick, AFTER camera.follow and BEFORE renderer.render
    - `game/Track.js` exposes `getSpeed()` returning an integer
    - `npm run build` exits 0
    - Visual smoke test: SCORE counter visibly ticks up while playing; SPEED visibly increases over the run (start ~20, ramps up); pixel font visible (Press Start 2P loaded).
  </acceptance_criteria>
  <done>HUD overlay shows SCORE and SPEED in pixel font, updates each frame with diff-guarded DOM writes, never blocks touch — HUD-01 and HUD-02 satisfied.</done>
</task>

<task id="01-03-T2" type="execute" tdd="false">
  <name>Task 2: Touch controls (multi-touch, pointer events) + keyboard fallback + visible press state</name>
  <files>index.html, style.css, game/Controls.js</files>
  <read_first>
    - .planning/research/PITFALLS.md #10 (passive touchstart blocks preventDefault — use pointerdown/pointerup) and #11 (multi-touch — track state per pointerId, never reset all state on any single pointer event) — these two are non-negotiable.
    - .planning/research/FEATURES.md "Mobile Touch Control Patterns" — Fixed large buttons bottom corners (40-50% screen width), visual press state, `touch-action: none` on controls overlay, large tap targets (>80px). Avoid small buttons.
    - inicio.md "Controles mobile" — "Dois botões grandes fixos na parte inferior", "feedback visual ao toque", "Touch events (touchstart/touchend)" — note: spec says touchstart but PITFALLS overrides to pointerdown for correctness.
    - game/Controls.js — current state from 01-01 is `{ left, right, forward }` keyboard only. This task REPLACES the existing implementation with a multi-input version: keyboard sets keyboard slots, touch sets touch slots, getIntent() ORs them. Forward is keyboard-only in Phase 1 (no on-screen forward button — car auto-advances when steering or via keyboard for desktop testing). The spec mentions an optional center turbo/brake button — defer to v2 per CLAUDE.md.
    - **Phase 1 design clarification:** The car needs to be moving for steering to be meaningful. Per inicio.md the original spec implies an endless runner where the car auto-advances; per requirements PHYS-01 says "Carro avança automaticamente". Apply this NOW: in `game/Car.js`, modify `applyInput(intent)` to ALWAYS apply forward force (auto-advance), with intent.forward (keyboard ArrowUp) as an additive turbo-ish boost. Without this change, mobile players with no forward button cannot drive.
  </read_first>
  <action>
    **Modify `game/Car.js`:** Change `applyInput(intent)` so the base forward force is ALWAYS applied (auto-advance, PHYS-01: "Carro avança automaticamente"). The keyboard `intent.forward` flag becomes a small additional boost for desktop testing only.
    ```
    applyInput(intent) {
      this.body.wakeUp();
      const baseForce = 2500;
      const boost = intent.forward ? 800 : 0; // keyboard testing convenience only
      this.body.applyLocalForce(new CANNON.Vec3(0, 0, -(baseForce + boost)), new CANNON.Vec3(0, 0, 0));
      if (intent.left)  this.body.torque.y += 400;
      if (intent.right) this.body.torque.y -= 400;
    }
    ```
    This change is what makes the mobile-only LEFT/RIGHT button layout viable.

    **Modify `index.html`:** Add a touch controls overlay AFTER the HUD div, BEFORE the gameOver overlay:
    ```html
    <div id="touchControls" class="touch-controls" aria-hidden="false">
      <button id="btnLeft" class="touch-btn touch-btn-left" type="button" aria-label="Steer left">◀</button>
      <button id="btnRight" class="touch-btn touch-btn-right" type="button" aria-label="Steer right">▶</button>
    </div>
    ```

    **Modify `style.css`:**
    ```
    .touch-controls {
      position: fixed;
      left: 0; right: 0; bottom: 0;
      height: 30vh;
      pointer-events: none; /* container does not block; buttons do */
      z-index: 75;
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      padding: 24px;
    }
    .touch-btn {
      pointer-events: auto;
      touch-action: none;     /* PITFALLS #10 — non-negotiable */
      user-select: none;
      -webkit-user-select: none;
      -webkit-tap-highlight-color: transparent;
      width: 30vw;
      max-width: 180px;
      min-width: 100px;
      min-height: 100px;
      max-height: 160px;
      background: rgba(0, 255, 255, 0.18);
      border: 3px solid #00ffff;
      color: #00ffff;
      font-family: 'Press Start 2P', monospace;
      font-size: 32px;
      border-radius: 16px;
      cursor: pointer;
      transition: transform 80ms ease-out, background 80ms ease-out;
    }
    .touch-btn.pressed {
      background: rgba(255, 238, 0, 0.4);
      border-color: #ffee00;
      color: #ffee00;
      transform: scale(0.92);
    }
    @media (max-width: 480px) {
      .touch-btn { font-size: 24px; }
    }
    /* Hide touch controls on desktop with no touch capability */
    @media (hover: hover) and (pointer: fine) {
      .touch-controls { display: none; }
    }
    ```
    The `(hover: hover) and (pointer: fine)` media query hides the buttons on mouse-only desktop browsers but keeps them on touch-capable desktops and all phones.

    **Rewrite `game/Controls.js`** with per-pointerId multi-touch state:
    ```
    export default class Controls {
      constructor() {
        this._keyState = { left: false, right: false, forward: false };
        // Map<pointerId, 'left' | 'right'>
        this._activePointers = new Map();
        this._bindKeyboard();
        this._bindTouch();
      }

      _bindKeyboard() {
        window.addEventListener('keydown', (e) => {
          if (e.key === 'ArrowLeft' || e.key === 'a' || e.key === 'A')   this._keyState.left = true;
          if (e.key === 'ArrowRight' || e.key === 'd' || e.key === 'D')  this._keyState.right = true;
          if (e.key === 'ArrowUp' || e.key === 'w' || e.key === 'W')     this._keyState.forward = true;
        });
        window.addEventListener('keyup', (e) => {
          if (e.key === 'ArrowLeft' || e.key === 'a' || e.key === 'A')   this._keyState.left = false;
          if (e.key === 'ArrowRight' || e.key === 'd' || e.key === 'D')  this._keyState.right = false;
          if (e.key === 'ArrowUp' || e.key === 'w' || e.key === 'W')     this._keyState.forward = false;
        });
      }

      _bindTouch() {
        const left = document.getElementById('btnLeft');
        const right = document.getElementById('btnRight');
        this._bindButton(left, 'left');
        this._bindButton(right, 'right');
      }

      _bindButton(el, dir) {
        el.addEventListener('pointerdown', (e) => {
          e.preventDefault();
          el.setPointerCapture(e.pointerId);
          this._activePointers.set(e.pointerId, dir);
          el.classList.add('pressed');
        });
        const release = (e) => {
          if (this._activePointers.get(e.pointerId) === dir) {
            this._activePointers.delete(e.pointerId);
          }
          // Only remove .pressed if no remaining pointer holds this button
          const stillHeld = Array.from(this._activePointers.values()).includes(dir);
          if (!stillHeld) el.classList.remove('pressed');
        };
        el.addEventListener('pointerup', release);
        el.addEventListener('pointercancel', release);
        el.addEventListener('pointerleave', release);
      }

      getIntent() {
        // Touch state derived from pointer map; keyboard ORed in
        const touchLeft  = Array.from(this._activePointers.values()).includes('left');
        const touchRight = Array.from(this._activePointers.values()).includes('right');
        return {
          left:  this._keyState.left  || touchLeft,
          right: this._keyState.right || touchRight,
          forward: this._keyState.forward,
        };
      }
    }
    ```
    Three crucial details: (1) `setPointerCapture` so a finger sliding off the button still keeps the steering active, (2) tracking per `pointerId` in a Map so left+right held simultaneously work independently (PITFALLS #11), (3) the `.pressed` class is only removed when NO active pointer still holds that direction.

    **Verify in `game/Game.js`:** No changes needed — Controls is already instantiated in 01-01. Confirm the constructor still does `this.controls = new Controls()` (no DOM-readiness issue, since main.js gates Game on DOMContentLoaded).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('fs');const c=[['Controls.js pointerdown',fs.readFileSync('game/Controls.js','utf8').includes('pointerdown')],['Controls.js per-pointerId Map',fs.readFileSync('game/Controls.js','utf8').includes('_activePointers')],['Controls.js setPointerCapture',fs.readFileSync('game/Controls.js','utf8').includes('setPointerCapture')],['Car.js auto-advance baseForce',fs.readFileSync('game/Car.js','utf8').includes('baseForce')],['index.html btnLeft and btnRight',fs.readFileSync('index.html','utf8').includes('id=\"btnLeft\"')&&fs.readFileSync('index.html','utf8').includes('id=\"btnRight\"')],['style.css touch-action none on touch-btn',fs.readFileSync('style.css','utf8').includes('touch-action: none')],['style.css .pressed press state',fs.readFileSync('style.css','utf8').includes('.touch-btn.pressed')]];let bad=0;c.forEach(([n,ok])=>{console.log(ok?'OK':'MISS',n);if(!ok)bad++});process.exit(bad?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - `game/Controls.js` uses `pointerdown` / `pointerup` / `pointercancel` (NOT `touchstart`/`touchend`)
    - `game/Controls.js` tracks state per `pointerId` in a Map called `_activePointers` (PITFALLS #11)
    - `game/Controls.js` calls `setPointerCapture(e.pointerId)` on pointerdown
    - `game/Car.js` `applyInput` always applies `baseForce` (auto-advance, PHYS-01) and adds `boost` only if intent.forward
    - `index.html` has `#btnLeft` and `#btnRight` buttons inside `#touchControls`
    - `style.css` has `touch-action: none` on `.touch-btn`, a `.touch-btn.pressed` class with visible visual change, and a `@media (hover: hover) and (pointer: fine)` rule hiding the buttons on desktop
    - Keyboard ArrowLeft/Right/Up still drives the car on desktop
    - `npm run build` exits 0
    - Touch buttons render with neon cyan style and switch to yellow .pressed visual when tapped (verified in T3 on real device)
  </acceptance_criteria>
  <done>Multi-touch buttons drive the car with visible press feedback. Keyboard still works on desktop. Auto-advance is on so steering alone produces forward motion. Press state visible. CTRL-01 and CTRL-03 satisfied in code. CTRL-02 (keyboard) preserved from 01-01.</done>
</task>

<task id="01-03-T3" type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mobile device verification — full Phase 1 success criteria on Android</name>
  <what-built>
    Phase 1 is complete in code: scaffold + physics + track + collision + retry + HUD + touch controls. This checkpoint is the Phase 1 hard gate from a real-device perspective: the developer connects an Android phone to the same Wi-Fi as the dev machine, opens the LAN URL printed by `npm run dev`, and verifies every Phase 1 success criterion on the actual device that the project targets.
  </what-built>
  <how-to-verify>
    Connect a real Android phone (or iPad if no Android available) to the same Wi-Fi network as the dev machine. Run `npm run dev` and note the **Network** URL printed (e.g., `http://192.168.x.x:3000/`). Open that URL in Chrome on the phone. Hold the phone in landscape orientation.

    **Check 1 — Game renders on device (Success Criterion #5 baseline):**
    1. Expected: cyan car visible above road, scrolling neon edges, yellow obstacles appearing, HUD visible at top, two large touch buttons at bottom corners.
    2. Open Chrome DevTools via remote debugging (chrome://inspect) on the dev machine. Check the console: no red errors. Check `renderer.info.render` (paste at the console): triangles within budget (car < 500, total scene reasonable).

    **Check 2 — HARD GATE: touch buttons drive the car (Success Criterion #1):**
    1. Tap and hold the LEFT button (◀).
    2. Expected: car visibly steers/rotates left and continues moving forward (auto-advance). Button switches to yellow .pressed visual.
    3. Release, tap and hold RIGHT (▶). Same behavior in the other direction.
    4. Tap and hold LEFT, then while still holding it, also tap RIGHT (multi-touch). Expected: both buttons show pressed state; car receives both torques and net steering may cancel or alternate — but the press states never get stuck "on" or "off" wrongly when releasing one of them.
    5. The car MUST NOT stay still under any sustained touch. If it does, one of PITFALLS #1, #2, #4, or #6 is back — STOP and report.

    **Check 3 — HUD visible and updating (Success Criterion #4):**
    1. While playing, glance at the top of the screen.
    2. Expected: SCORE counter ticks up continuously. SPEED counter starts around 20 and increases over ~30s of play. Both in pixel font (Press Start 2P loaded — if it shows fallback monospace, network may have blocked the font load; note it but do not fail).

    **Check 4 — Track scrolls, lateral limits hold, no fall-through (Success Criterion #2):**
    1. Watch road segments scrolling past — continuous, no gaps.
    2. Tap LEFT hard until the car hits the wall — it should stop at the edge, not fall off, not flip.
    3. Same right. Car never falls below the ground plane.

    **Check 5 — Collision → Game-Over → Retry (Success Criterion #3):**
    1. Deliberately steer into a yellow cube.
    2. Game-Over overlay appears with SCORE. RETRY button is large and tappable.
    3. Tap RETRY. Expected: overlay dismisses, car returns to spawn, you can immediately steer and play again — sub-second perceived latency, no page reload (URL bar shows no reload icon spin).
    4. Repeat the crash → retry cycle 5 times. No degradation, no console errors.

    **Check 6 — Performance (Success Criterion #5):**
    1. In Chrome remote DevTools, open the Performance tab and record ~10 seconds of gameplay.
    2. Expected: average FPS ≥ 30 (target 60). If below 30, capture which subsystem dominates (rendering vs. physics vs. JS) and report — this is the only condition where Phase 1 still "ships" but a tuning task may be scheduled into Phase 2.

    **Check 7 — Tab switch / visibilitychange (carried from 01-01):**
    1. Switch to another app on the phone for 10 seconds, then return.
    2. Expected: game resumes without flying off-screen, without NaN positions, and rendering picks back up normally.

    Report PASS for all seven, or specify which checks failed with observed behavior, FPS numbers, and any DevTools console errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all seven checks pass on the real Android device. Otherwise describe failures — Phase 1 is not "complete" until this on-device verification clears.</resume-signal>
</task>

</tasks>

<verification>
Plan 01-03 succeeds when a real Android device opened on the LAN dev URL renders the game, the touch buttons drive the car visibly, the HUD shows live SCORE and SPEED in pixel font, the crash → retry loop works without page reload, the FPS holds at 30+ on mid-range hardware, and the game survives backgrounding without delta explosion. This is also the Phase 1 success criteria gate — passing this means Phase 1 is shippable.
</verification>

<success_criteria>
- All seven T3 acceptance checks pass on a real Android device
- All five Phase 1 success criteria from ROADMAP are visibly satisfied
- All Phase 1 requirements (PHYS-01..04, CTRL-01..03, TRACK-01..04, GAME-01..04, HUD-01..02, UI-03, PERF-01..04) are implemented and verified
</success_criteria>

<output>
Create `.planning/phases/01-physics-foundation-and-core-gameplay-loop/01-03-SUMMARY.md` when done. Include: device tested (make/model/Android version), observed FPS range, any visual quirks (font fallback, button positioning at unusual aspect ratios), and a short list of Phase 2 candidates discovered during testing (anything that "should have" worked but didn't — to feed into Phase 2 hardening backlog).
</output>
